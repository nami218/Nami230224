//Thư mục cha
// => Gọi tên thư mục con + đặt tên(tự đặt) = {truyền giá trị vào}

// Thư mục con
// Truyền giá trị props hoặc {} vào function ban đầu
// Tại mục biến: 
// => props.tên tự đặt
// => {}: sử dụng trực tiếp tên đã rã